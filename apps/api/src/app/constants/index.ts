/**
 * The interval of how often the client socket is updated.
 */
export const INTERVAL = 1000;
/**
 * The duration (in seconds) of the sliding window for the
 * time series chart data.
 */
export const TEN_MINUTES = 600;
